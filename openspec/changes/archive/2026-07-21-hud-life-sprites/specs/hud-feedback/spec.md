## MODIFIED Requirements

### Requirement: Lives display
While playing, the HUD SHALL show the current remaining lives **using life/heart sprites** (not only a numeric text label).

#### Scenario: Lives visible as icons
- **WHEN** the match is in the playing state
- **THEN** remaining lives are represented by visible life sprites on screen
